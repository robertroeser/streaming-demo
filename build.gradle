subprojects {
    apply plugin: 'idea'

    group 'io.rsocket.demo'
    version '1.5.3'

    ext {
        log4jVersion = '2.9.1'
        picocliVersion = '3.9.5'
    }

    repositories {
        jcenter()
    }
}